{
  "name": "pop-balloons-bcard",
  "devDependencies": {
    "@babel/core": "^7.10.3",
    "@babel/preset-env": "^7.10.3",
    "babel-preset-env": "^1.7.0",
    "gh-pages": "^3.1.0",
    "parcel": "^1.12.4",
    "sass": "^1.26.9"
  },
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url /pop-balloons-bcard",
    "deploy": "gh-pages -d dist"
  }
}
